//! This is documentation for the `webhotel` lib crate
//! 
//! Usage:
//! ```
//!     let web=webhotel::new();
//! ```
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
